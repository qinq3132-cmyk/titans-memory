# Titans Memory 项目解读

> 这份文档用于沉淀当前对仓库的代码解读。内容会保持精简、面向阅读；在本次对话继续推进时，我可以继续增量更新它。

## 1. 项目定位

- 这是 `titans-pytorch` 的一个实现仓库，对应论文 `Titans: Learning to Memorize at Test Time`。
- 项目的目标不是给出完整 benchmark 报告，而是提供可运行、可复用的实现。
- README 里提供了安装方式、最小使用示例、训练入口和参考论文，但**没有给出具体实验结果或复现表格**。

## 2. 当前仓库的重点

可以把整个项目分成三层：

```text
memory_models.py      记忆网络骨架
neural_memory.py      神经记忆机制
mac_transformer.py    完整可训练模型（MAC Transformer）
```

当前判断：

- `memory_models.py` 是底层组件，定义“记忆网络长什么样”。
- `neural_memory.py` 是核心机制，负责“怎么写入 / 读取记忆”。
- `mac_transformer.py` 是最主要的落地架构，负责把局部注意力和神经记忆结合成完整模型。

## 3. README 的真实信息量

README 主要告诉我们三件事：

1. **怎么安装**：`pip install titans-pytorch`
2. **怎么调用**：分别给出 `NeuralMemory` 和 `MemoryAsContextTransformer` 的最小示例
3. **怎么实验**：提示修改并运行 `train_mac.py`

README **没有**给出：

- loss / perplexity / accuracy 等实验数值
- 与原论文的复现对比
- 收敛曲线、表格、图表
- 完整的数据和训练结论

因此这个仓库更像是**研究实现 / 工具库**，而不是“论文结果展示仓库”。

## 4. `NeuralMemory` 和 `MAC Transformer` 的关系

### 4.1 `NeuralMemory` 是什么

`NeuralMemory` 的核心思想是：

- 不把历史信息存成不断增长的 KV cache
- 而是把历史压缩进一个小型记忆网络（通常是 MLP）的**权重**里

它的工作流程可以概括为：

```text
store:
  token -> key / value
  用 |M(key) - value|^2 的梯度更新记忆网络权重

retrieve:
  token -> query
  用 query 直接喂给记忆网络，得到检索结果
```

所以它更像一个“固定容量、可写入的有损记忆体”。

### 4.2 它能不能直接解决 Transformer 的 KV cache 问题

**不能靠“简单插在 attention 后面”来解决。**

例如：

```python
x = my_attention_layer(x)
x, state = mem(x, state = state)
x = my_ffn_layer(x)
```

这里 `my_attention_layer` 自己的 KV cache 仍然还在，长度仍然会增长，所以 cache 问题没有被根除。

### 4.3 它能不能完全替代 Attention

**理论上可以承担“从历史中检索信息”的角色，但实践上不能完全等价替代 attention。**

原因是：

- Attention 是对历史 token 的相对精确检索
- `NeuralMemory` 是把历史压缩进固定大小参数中的**有损压缩**

所以：

- 近距离、细粒度信息：attention 更强
- 超长距离、压缩语义信息：`NeuralMemory` 更省内存

### 4.4 为什么 `MAC Transformer` 才是更合理的主角

`MAC Transformer` 采用的是混合思路：

- **局部 attention**：负责近距离精确信息
- **NeuralMemory**：负责长距离压缩记忆

也就是：

```text
短程依赖 -> local attention
长程依赖 -> neural memory
```

这也是当前仓库里最完整、最重要的模型形态。

## 5. 为什么说 `mac_transformer.py` 是项目主体

原因有三：

- 它是一个完整可训练、可采样、可推理的模型，而不只是机制组件。
- `train_mac.py` / `train_mac_multigpu.py` 训练的对象就是它。
- 它把项目真正想表达的结构落地了：`local attention + long-term neural memory`。

换句话说：

- `NeuralMemory` 是“方法”
- `MemoryAsContextTransformer` 是“产品化的模型架构”

## 6. `memory_models.py` 代码逻辑梳理

这个文件定义了“记忆网络本体”的几种可选结构。它们都会被 `NeuralMemory` 拿来当作可写入的记忆容器。

### 6.1 基础组件

#### `LayerNorm`

- 自定义 layer norm
- 使用 `nn.LayerNorm(..., elementwise_affine = False)`
- 自己维护 `gamma` 参数

#### `ResidualNorm`

逻辑是：

```python
out = model(x)
return norm(out) + x
```

这是 TTT 风格的“模型输出 + 归一化 + 残差”包装器。

### 6.2 主要记忆模型

#### `MemoryMLP`

默认记忆模型。

逻辑：

```text
x -> x @ W1 -> GELU -> x @ W2 -> ... -> output
```

特点：

- 最朴素
- 参数直接由 `ParameterList` 管理
- 是默认的 memory backbone

#### `GatedResidualMemoryMLP`

在每层里做门控残差混合。

逻辑：

```text
hidden = GELU(x @ W1) @ W2
branch_out = hidden
gate = sigmoid([branch_out, residual] @ W_gate)
x = lerp(residual, branch_out, gate)
```

特点：

- 表达力更强
- 能控制“保留旧信息 / 注入新信息”的比例

#### `FactorizedMemoryMLP`

对每层权重做低秩分解。

逻辑：

```text
x @ W1(dim x k) @ W2(k x dim)
```

特点：

- 更省参数
- 更适合在小 chunk / 小内存约束下试验

#### `MemorySwiGluMLP`

借鉴现代 Transformer FFN 的 SwiGLU 结构。

逻辑：

```text
x_proj, gate = (x @ W1).chunk(2)
out = (x_proj * GELU(gate)) @ W2
x = x + out
最后做 LayerNorm
```

特点：

- 更现代的 FFN 风格
- 内部自带残差，更稳定

#### `MemoryAttention`

用一个小 attention block 作为记忆网络。

逻辑：

- 先计算 `q / k / v`
- 用 `scaled_dot_product_attention(..., is_causal = True)` 做因果注意力
- 再并联一个前馈支路
- 输出为 `attn_out + ff_out`

特点：

- 表达力强
- 但比纯 MLP 更重

### 6.3 这个文件的定位总结

`memory_models.py` 不负责“记忆如何更新”，只负责“记忆网络长什么样”。

更准确地说：

- 这里定义的是**记忆容器结构**
- `neural_memory.py` 负责把这些容器变成“可读写的记忆系统”
- 这些容器本质上就是**长期记忆模型（long-term memory model）的不同候选实现**
- 原始 TTT / Titans 路线默认使用的是简单 MLP；这个文件则把若干可能的变种一起实现出来，方便替换和实验

## 7. 当前测试结论（对代码可靠性的参考）

在当前环境里跑 `tests/test_titans.py` 的结果是：

- 绝大多数测试通过（`9805 passed`）
- 唯一失败是 `test_assoc_scan[True]`
- 失败原因不是主逻辑错误，而是 CUDA 扩展编译时缺少 `cusparse.h`

因此当前可以认为：

- 代码主体逻辑基本可用
- 加速扫描路径依赖本机 CUDA 开发环境是否完整

## 8. `neural_memory.py` 代码逻辑梳理

这个文件是项目真正的机制核心：它把 `memory_models.py` 里的记忆网络骨架，变成一个可以被持续写入、持续读取、可跨 chunk 续接状态的长期记忆系统。

可以把它拆成四层：

```text
辅助函数 / 状态定义
   ↓
NeuralMemory.__init__      配置记忆系统
   ↓
store_memories            写入记忆（更新 memory weights）
   ↓
retrieve_memories         读取记忆（用 query 检索）
   ↓
forward                   串起 state / chunk / 单 token 推理
```

### 8.1 状态定义

`NeuralMemState` 里保存了 5 个字段：

- `seq_index`：当前已经处理到原序列的哪个位置
- `weights`：当前记忆网络的基础权重
- `cache_store_segment`：还没凑满一个完整 store chunk 的残余片段
- `states`：优化状态，主要是上一次 update 和 momentum
- `updates`：本次序列上每个 chunk 对应的更新后权重轨迹

这说明 `NeuralMemory` 不是无状态层，而更像一个“带内部记忆状态的序列模块”。

更细一点看：

- `seq_index`
  - 表示当前 store 指针已经走到了全局序列的哪个位置
  - 作用是让下一次调用时，chunk 对齐、batch boundary 对齐、`prev_weights` 对齐都不会错位

- `weights`
  - 表示当前“基准记忆权重”
  - 它不是每个 token 的瞬时轨迹，而是下一轮 store 从哪里继续写的起点参数
  - 当设置了 `batch_size` / `ttt_batch_size` 时，只有到边界时才会真正提交为新的 base weights

- `cache_store_segment`
  - 表示上一次调用结束时，没凑满 `store_chunk_size` 的尾巴
  - 因为 `store_memories` 只会对完整 chunk 执行写入，所以残余片段先缓存，等下一次输入来了再拼接起来继续写

- `states`
  - 这是优化器状态，不是模型输出状态
  - 具体是一个二元组：`(last_update, last_momentum)`
  - `last_update` 是上一轮每个参数张量最后一个 chunk 的更新值
  - `last_momentum` 是高阶 momentum 的末状态
  - 下一次 `store_memories` 会把它们当作递推初值继续做 associative scan

- `updates`
  - 这是当前这次 forward 过程中生成的“整条更新轨迹”
  - 形状上它包含每个 chunk 对应的权重版本，而不只是最后一个版本
  - `retrieve_memories` 会按位置取对应的那一份参数来做读取
  - 所以它更像“本次序列用来读记忆的时间展开权重”，而不是跨调用长期保存的 base state

### 8.2 初始化阶段在做什么

`__init__` 的核心工作不是普通建层，而是在配置一套“记忆读写系统”：

1. **确定 memory model**
  - 默认用 `MemoryMLP`
  - 也可以替换成 `MemoryAttention` 等其他骨架
  - 可选再包一层 `ResidualNorm`

2. **把 memory model 的参数当作记忆本体**
  - `self.memory_model_parameters` 存的就是这套长期记忆
  - 后面写入记忆，本质就是更新这些参数

3. **建立读写投影**
  - `to_queries`：把输入投成 query，用于读取
  - `to_keys` / `to_values`：把输入投成 key / value，用于写入

4. **建立“学习如何更新记忆”的控制头**
  - `to_adaptive_step`：每个位置生成自适应学习率
  - `to_momentum`：生成动量因子
  - `to_decay_factor`：生成遗忘 / 衰减因子
  - `to_layer_modulation`：可对不同参数张量施加不同学习率调制

5. **准备可微的 per-sample 更新函数**
  - 通过 `torch.func.functional_call + grad + vmap`
  - 对每个样本 / 每个 chunk 计算“如果让记忆网络拟合当前 key-value，会得到什么梯度”

这一步非常关键：它让“梯度”本身变成了记忆写入信号。

更细一点：

- `functional_call`
  - 含义是：**不用模块自己内部当前挂着的参数，而是显式传入一套参数来执行前向**
  - 在这里相当于：`memory_model(inputs; params = 某一时刻的记忆权重)`
  - 这样 memory weights 就可以像普通张量一样被外部替换、复制、展开和求梯度

- `grad`
  - 含义是：**对某个标量损失函数求梯度**
  - 这里求的是“当前 memory model 参数如果要把 `key -> value` 拟合好，它们应该往哪个方向变化”
  - 求出来的梯度后面会取负号，当成记忆写入信号（`surprise`）

- `vmap`
  - 含义是：**把一个“只会处理单个样本”的函数，自动向量化成可并行处理一批样本的函数**
  - 这里是把“单个样本、单个 chunk 的梯度计算”扩展成“整批样本、整批 chunk 一次算完”
  - 所以它服务的是并行性和效率，不改变算法本质

把三者合起来就是：

```text
给定某一份 memory weights
  -> 用 functional_call 执行 memory model 前向
  -> 用 grad 对 loss 求参数梯度
  -> 用 vmap 把这个过程批量化到每个样本 / 每个 chunk
```

因此这里并不是在调用普通优化器，而是在模型内部自己构造了一套“显式的、可批量化的 test-time 更新规则”。

### 8.3 `store_memories`：怎么把序列写进长期记忆

`store_memories` 的思路可以概括为：

```text
输入序列
  -> 生成 key / value
  -> 用 memory model 做一次拟合损失 M(key) ~ value
  -> 对 memory weights 求梯度
  -> 把负梯度当成 surprise / update
  -> 再叠加动量、衰减、分层调制
  -> 得到每个 chunk 的更新后权重轨迹
```

更细一点：

1. **序列按 `store_chunk_size` 截断并分 chunk**
  - 只有完整 chunk 才参与记忆写入
  - 不完整尾巴先缓存到 `remainder`

2. **输入先做 store norm**

3. **生成优化超参数**
  - token 级别的 `adaptive_lr`
  - chunk 级别的 `decay_factor`
  - 可选 `momentum`

4. **生成 key / value**
  - `to_keys(seq)`、`to_values(seq)`
  - 再展开成多头、按 chunk 重排

5. **构造记忆拟合损失**
  - 本质是 `loss = |M(key) - value|^2`
  - 用 `per_sample_grad_fn` 对记忆参数求梯度

6. **把梯度解释成 surprise**
  - `surprises = - grads`
  - 这是写入长期记忆的核心更新信号

7. **在 surprise 上继续做“优化器逻辑”**
  - 可选梯度裁剪
  - 可选每层学习率调制
  - 可选高阶动量（通过 `AssocScan`）
  - 可选谱归一化
  - 可选 learned decay / forgetting

8. **输出不是单一权重，而是一条 update 轨迹**
  - 每个 chunk 都会对应一组更新后的参数
  - 这些参数轨迹后面会被拿来给 retrieval 使用

所以 `store_memories` 做的事情，不是传统“把 K/V 追加到缓存”，而是：

- 让记忆网络临时拟合当前 chunk
- 把拟合过程中得到的参数更新，视作长期记忆写入

把代码结构再细分，可以拆成四段：

#### A. 输入整理与 chunk 对齐

- 先根据 `store_chunk_size` 把序列向下截断到完整 chunk 数
- 不完整尾巴保存到 `remainder`
- 更新 `next_seq_len_index`
- 如果没有传入 `weights`，就从初始化 memory weights 开始

这一步的目的，是保证后续所有写入都发生在“完整 chunk”上。

#### B. 为“写入更新”生成控制信号

这一段会从输入序列里额外生成一组优化控制量：

- `adaptive_lr`：token 级别的自适应学习率
- `chunked_seq`：先把一个 chunk 压成一个代表向量
- `decay_factor`：chunk 级别的遗忘系数
- `adaptive_momentum`：chunk 级别的动量因子
- `layer_lr_mod`：不同 memory 参数张量的更新缩放

与此同时，还会生成真正的写入内容：

- `keys = to_keys(seq)`
- `values = to_values(values_seq)`

然后把它们按多头和 chunk 重新排布，方便后续批量求梯度。

#### C. 把“拟合 key-value”变成梯度

核心操作是：

```text
用当前 memory weights 对 key 做前向
看它与 target value 的误差
对 memory weights 求梯度
```

代码上就是：

- `self.per_sample_grad_fn(...)`
- 输出 `grads`
- 再取负号得到 `surprises = -grads`

这里的 `surprise` 可以理解为：

> 当前 chunk 告诉长期记忆“你应该往哪个方向更新，才能更好记住我”

#### D. 用递推规则把 `surprise` 变成 `updates`

有了 `surprise` 之后，并不会直接把它当最终 update，而是还要经过一层“内部优化器”逻辑：

- 可选高阶 momentum（`AssocScan` 递推）
- 可选 learned momentum combination
- 可选谱归一化
- learned decay / forgetting（再次 `AssocScan`）

最后得到：

- `updates[param_name]`：每个 chunk 对应的权重轨迹
- `next_last_update`：最后一个 chunk 的更新状态
- `next_last_momentum`：最后一个 chunk 的动量状态

然后把这些打包进：

- `next_state = (next_last_update, next_last_momentum)`
- `next_store_state = NeuralMemState(...)`

这说明 `store_memories` 的输出其实有两层：

1. **给当前序列 retrieval 用的整条权重轨迹**（`updates`）
2. **给下一次继续写入用的末状态**（`next_store_state`）

### 8.4 `retrieve_memories`：怎么从长期记忆里读信息

读取路径相对直接：

```text
输入序列
  -> to_queries
  -> 分头 / 分 chunk
  -> 用当前 memory weights 对 query 做 functional_call
  -> 得到 retrieved values
```

关键点：

- 这里没有 attention，也没有显式 KV cache
- 检索就是“把 query 喂给当前 memory model”
- 如果是多 chunk / 多时刻的权重轨迹，就为不同位置使用对应时刻的 memory weights

最终输出形状和输入序列对齐，因此从外部看它很像一个普通 sequence layer。

### 8.5 `forward`：怎么把 store、retrieve、state 串起来

`forward` 是整个模块的调度器，负责处理三类现实问题：

1. **单 token 推理**
  - 如果输入只有一个 token，会自动切到单步模式

2. **跨调用续接状态**
  - 通过 `state` 把上次未完成的 chunk、上次的 momentum、上次的权重更新延续下来

3. **按 batch boundary 更新真实权重**
  - 如果设置了 `batch_size` / `ttt_batch_size`
  - 不会每个 token 都立刻把 base weights 覆盖掉，而是在边界处再提交更新

整个 `forward` 的流程是：

```text
收到 seq
  -> 衔接上次残余 store_seq
  -> 按 batch 边界切分 store 段
  -> 对每一段调用 store_memories
  -> 汇总 updates / surprises
  -> 用得到的 updates 调用 retrieve_memories
  -> 返回 retrieved 与 next_state
```

因此它既负责“写”，又负责“读”，同时还负责管理长期记忆的连续时间状态。

### 8.6 对 `NeuralMemory` 的一句话总结

`NeuralMemory` 可以理解成：

> 一个把“小记忆网络参数”当作外显记忆载体、并用测试时梯度更新来写入记忆的序列模块。

它不像 attention 那样保存全量历史，而是把历史压缩为不断演化的参数状态。

## 9. `mac_transformer.py` 代码逻辑梳理

这个文件不是在重新定义长期记忆，而是在做更高一层的事情：

> 把 `NeuralMemory` 作为 long-term memory 模块，接到完整 Transformer 里，与局部 attention 一起组成 MAC（Memory As Context）架构。

可以把它拆成四层：

```text
辅助函数 / 采样工具
   ↓
SegmentedAttention              局部窗口注意力
   ↓
MemoryAsContextTransformer      把 long-term memory + local attention + FFN 组起来
   ↓
sample / forward                训练与自回归推理路径
```

### 9.1 整体结构

文件里最重要的两个类是：

- `SegmentedAttention`
  - 负责局部注意力
  - 支持 persistent memory、sliding window、FlexAttention、KV cache

- `MemoryAsContextTransformer`
  - 完整模型主体
  - 负责把 token embedding、long-term memory token、attention、feedforward、`NeuralMemory` 串起来

除此之外还有几个关键辅助函数：

- `create_mac_block_mask`：给 FlexAttention 构造 MAC 的块状 mask
- `pad_and_segment_with_inverse`：把序列按 segment 切开，并提供恢复函数
- `min_p_filter` / `gumbel_sample`：采样相关工具

### 9.2 `SegmentedAttention`：局部注意力层

`SegmentedAttention` 的定位非常明确：

- 它不是全局 attention
- 它只看一个 segment（或相邻 segment，若开启 sliding）
- 同时还可以额外挂上 persistent memory token

主要逻辑：

1. 输入先做 `RMSNorm`
2. 用 `to_qkv` 生成局部 attention 的 `q/k/v`
3. 可选把上一层的 `value_residual` 混进来
4. 加上 rotary embedding
5. 按 segment 切块
6. 在每个 segment 内做 causal attention
7. 把 persistent memory token 的 key/value 拼接到局部窗口前面

它支持三种运行模式：

- `forward_inference`：单 token 推理，配合 KV cache
- `forward_flex`：在 CUDA + 新版 PyTorch 下用 FlexAttention
- `forward`：常规训练 / 推理路径

所以这个类可以看成：

> 一个“只负责短程依赖”的 attention 模块。

### 9.3 `MemoryAsContextTransformer`：长期记忆怎么接进模型

这个类的设计思想是：

```text
短程依赖 -> SegmentedAttention
长程依赖 -> NeuralMemory
两者一起构成每层的上下文来源
```

在 `__init__` 里，模型会建立：

1. **token embedding**
2. **轴向位置编码**（帮助区分 segment 内 / segment 间位置）
3. **long-term memory tokens**
  - 这是显式插入序列中的长期记忆占位 token
4. **每层的 3 个分支**
  - `mem`：`NeuralMemory`
  - `attn`：`SegmentedAttention`
  - `ff`：FeedForward
5. **hyper connection 机制**
  - 负责多 residual stream 的展开和回收
  - 也允许 memory 从不同中间层视图里选择 q/k/v 输入

每层结构可以粗略理解成：

```text
x
 -> NeuralMemory   （提取 long-term memory）
 -> SegmentedAttention （处理 local context）
 -> FeedForward
```

其中一个很重要的点是：

- `NeuralMemory` 不直接替代 attention
- 它提供的是“长期记忆检索结果”
- 这些结果要么直接加回 residual stream
- 要么作为 gate，去调制 attention 输出

### 9.4 长期记忆 token 是怎么插进去的

`forward` 一开始会先做：

1. token embedding
2. 按 `segment_len` 对序列切段
3. 在每段之间插入 `num_longterm_mem_tokens` 个可学习记忆 token
4. 再加轴向位置编码

于是模型真正看到的序列不是原始 token 序列，而是：

```text
[segment tokens] + [long-term mem tokens] + [segment tokens] + [long-term mem tokens] + ...
```

这就是“Memory As Context”这个名字的来源：

- 长期记忆不是单独开一条分支最后再融合
- 而是作为上下文的一部分，插进每个 segment 的建模过程中

### 9.5 每层 forward 的执行顺序

在主 `forward` 里，每层基本按这个顺序跑：

1. **NeuralMemory 分支**
  - 从当前 residual stream（或若干历史层视图）生成 memory 输入
  - 调用 `mem.forward(...)` 得到 `retrieved`
  - 可选把 `retrieved` 当成 attention output gate
  - 否则直接加回 residual stream

2. **局部 attention 分支**
  - 对当前表示做 segment-level attention
  - 保留 `value_residual`
  - 维护 KV cache

3. **FeedForward 分支**
  - 普通 transformer FFN

因此每层都同时拥有：

- 局部注意力上下文
- 长期记忆检索结果
- 普通非线性变换

### 9.6 训练路径与推理路径

#### 训练 / 全序列 forward

`forward(x, return_loss = True)` 时：

- 输入 token ids
- 自动右移构造 labels
- 插入 long-term memory tokens
- 全层跑完后输出 logits
- 最后做交叉熵损失

#### 自回归推理

推理时支持 cache：

- attention 使用 `kv_caches`
- neural memory 使用 `neural_mem_caches`
- `sample()` 每次只生成一个 token

还有一个很特别的点：

- 推理过程中遇到“long-term memory token 位置”时，模型只更新 cache，不产出 logits
- 因为这些位置只是内部记忆上下文，不对应真实输出 token

### 9.7 对 `mac_transformer.py` 的一句话总结

`mac_transformer.py` 可以理解成：

> 一个把局部 attention 负责短程依赖、把 `NeuralMemory` 负责长程依赖，并通过 memory tokens 把两者统一到同一上下文中的完整 Transformer 架构。

## 10. 后续解读建议

接下来比较值得继续梳理的文件：

1. `train_mac_multigpu.py`：训练脚本如何真正驱动这个模型
2. `implicit_mlp_attention.py` / `nested_attention.py`：这些探索性模块与主线的关系

---

如果后面我们继续聊这个仓库，我可以直接把新的结论继续补到这份 `项目解读.md` 里。
